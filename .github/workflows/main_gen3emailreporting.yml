name: Build and deploy Node.js app with OpenTelemetry tracing

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      trace_id: ${{ steps.start_trace.outputs.trace_id }}
      parent_span_id: ${{ steps.start_trace.outputs.parent_span_id }}
    steps:
      - name: 🧾 Checkout Code
        uses: actions/checkout@v4

      - name: ⬇️ Install Trace CLI Dependencies
        run: |
          npm install @opentelemetry/sdk-node \
                      @opentelemetry/api \
                      @opentelemetry/resources \
                      @opentelemetry/semantic-conventions \
                      @opentelemetry/exporter-trace-otlp-proto \
                      node-fetch \
                      dotenv \
                      winston

      - id: start_trace
        name: 🚀 Start Build Trace
        run: |
          node trace-cli.js start --step=build > trace_output.txt
          echo "trace_id=$(grep '^trace_id=' trace_output.txt | head -1 | cut -d '=' -f2 | xargs)" >> $GITHUB_OUTPUT
          echo "parent_span_id=$(grep '^parent_span_id=' trace_output.txt | head -1 | cut -d '=' -f2 | xargs)" >> $GITHUB_OUTPUT

      # Child step (example: Install Dependencies)
      - id: install_start
        name: 📦 Install Dependencies (Start)
        run: |
          node trace-cli.js start-child --step="Install Dependencies" \
            --trace-id=${{ steps.start_trace.outputs.trace_id }} \
            --parent-span-id=${{ steps.start_trace.outputs.parent_span_id }} > output.txt
          echo "span_id=$(grep '^span_id=' output.txt | head -1 | cut -d '=' -f2 | xargs)" >> $GITHUB_OUTPUT

      - name: 📦 Install Dependencies (Execute)
        run: npm install

      - name: 📦 Install Dependencies (End)
        run: |
          node trace-cli.js end-child --step="Install Dependencies" \
            --trace-id=${{ steps.start_trace.outputs.trace_id }} \
            --span-id=${{ steps.install_start.outputs.span_id }}

      # Repeat for each build/test/zip/etc step (same as above, always passing parent_span_id from start_trace step)

      - name: 🛑 End Build Trace
        run: |
          node trace-cli.js end --step=build \
            --trace-id=${{ steps.start_trace.outputs.trace_id }} \
            --parent-span-id=${{ steps.start_trace.outputs.parent_span_id }}

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: 🧾 Checkout Code
        uses: actions/checkout@v4

      - name: ⬇️ Install Trace CLI Dependencies
        run: |
          npm install @opentelemetry/sdk-node \
                      @opentelemetry/api \
                      @opentelemetry/resources \
                      @opentelemetry/semantic-conventions \
                      @opentelemetry/exporter-trace-otlp-proto \
                      node-fetch \
                      dotenv \
                      winston

      - id: start_trace
        name: 🚀 Start Deploy Trace
        run: |
          node trace-cli.js start --step=deploy > trace_output.txt
          echo "trace_id=$(grep '^trace_id=' trace_output.txt | head -1 | cut -d '=' -f2 | xargs)" >> $GITHUB_OUTPUT
          echo "parent_span_id=$(grep '^parent_span_id=' trace_output.txt | head -1 | cut -d_
